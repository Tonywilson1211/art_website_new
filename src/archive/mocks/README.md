# Archived homepage mocks (Options 1–10)

These components are kept for design reference only. They are **not** used by the live site.

The production website is built from Mock 7 (Sketchbook Studio) in `src/pages/` and `src/components/layout/`.

To preview an old mock again temporarily, import it into `App.tsx` — do not ship that to production.
