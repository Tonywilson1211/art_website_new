name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - run: npm ci
      - run: npm run build

      - name: Verify production build (not dev index.html)
        run: |
          if grep -q 'src/main.tsx' dist/index.html; then
            echo "::error::dist/index.html still references src/main.tsx — build did not produce a production bundle."
            exit 1
          fi
          if ! grep -q '/art_website_new/assets/' dist/index.html; then
            echo "::error::dist/index.html missing /art_website_new/assets/ paths — check vite base config."
            exit 1
          fi
          echo "Build output verified."

      # Pushes ./dist to gh-pages branch — serve this branch in GitHub Pages settings.
      - name: Deploy dist to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          cname: false
